.progress-bar-root
  display flex
  flex-direction row
  height 5px
  justify-content flex-end
  margin 0 0 60px

  .indicator
    height 100%
    background-color var(--primary-color)
    border-radius 5px
    transition width 0.3s ease-in-out
